# The session-end sound is ~4s long and is played while a "delay" shutdown
# inhibitor is held, so logind must be willing to wait at least that long
# before proceeding with a shutdown. The default is 5s, which is cutting it
# close for a 3.9s sound plus aplay startup.
[Login]
InhibitDelayMaxSec=10
